{
  "name": "htz-json-scheme",
  "version": "0.0.1",
  "description": "An interface for an Haaretz JSON scheme",
  "main": "src/index.d.ts",
  "repository": "git@github.com:Haaretz/htz-json-scheme.git",
  "author": "TxHawks <tofu.hawks@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "typedoc": "^0.7.1"
  },
  "scripts": {
    "docs": "typedoc --name htz-json-scheme --target ES6 --excludeExternals --includeDeclarations --out docs/ src/ && touch docs/.nojekyll"
  }
}
